# Database Configuration
DATABASE_URL=postgresql://postgres:your_password@localhost:5432/trading_simulator

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-to-random-string
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# Application Settings
APP_NAME=Trading Strategy Simulator
APP_VERSION=1.0.0
DEBUG=True

# Stock Data Configuration
STOCK_DATA_CACHE_TTL=86400  # 24 hours in seconds
MAX_BACKTEST_YEARS=10
DEFAULT_INITIAL_CAPITAL=100000

# Performance Settings
MAX_WORKERS=4
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20

# API Rate Limiting
RATE_LIMIT_PER_MINUTE=60
RATE_LIMIT_PER_HOUR=1000

# Note: Generate a random JWT secret key with:
# python -c "import secrets; print(secrets.token_urlsafe(32))"
